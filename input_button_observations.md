# Input Button Observations — March 20, 2026

## Current State
- Input bar at bottom: element 15 (+ button), element 16 (textarea), element 17 (audio toggle), element 18 (mic button)
- The unified mic/send button is working — shows mic icon when textarea is empty (element 18)
- Audio toggle (speaker icon) is visible as element 17, positioned left of the mic button
- The header no longer has a duplicate audio toggle — only the hands-free phone button remains in header (element 10)
- The + button (element 15) opens the context menu

## What's Working
- + button for context menu (attach file, image, visual, go live)
- Audio toggle next to unified button
- Mic icon shows when empty
- Need to verify: send icon shows when text is typed
