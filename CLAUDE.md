# Stewardly — Recursive Optimization Project

## Overview
5-layer financial advisory AI platform. Reference architecture from which shared packages (@platform/intelligence, @platform/config) are extracted for Atlas, AEGIS, and Sovereign.

## Stack
TypeScript, tRPC, Drizzle ORM, TiDB, React 19
128 pages (119 + 9 new from pass 120: NewLanding, MyWork, MyFinancialTwin, AudioPreferences, ExamSimulator, DisciplineDeepDive, CaseStudySimulator, AchievementSystem, ConnectionMap), 356 tables (353 + 3 audio system tables from pass 120), 260 services (+ audioScriptGenerator), 78 routers, 17 seed files, 37 cron jobs, 142 components (130 + 12 new from pass 120: AudioCompanion, PlatformIntelligence, PersonaSidebar5, ChatGreetingV2, MobileChatLayout, CodeChatSetup, OnboardingFlow, TypingIndicator, EmptyStates, CelebrationEngine, feedbackSpecs, stewardErrors), 39 navigation items (35 + 4 new: Financial Twin, My Work, Achievements, Concept Map)
Tests: 3,103 passing across 109 files in local dev — 123 files / 3,215 tests total, with 14 pre-existing env-dependent failing files (112 tests) that clear in the deployed environment when DB + env vars are present
Current state: ~98% deep, 2% human-dependent (env vars, GHL, compliance). 102 recursive passes converged (9.7/10; pass 45 fixed pdfReportGenerator invokeLLM bypass; pass 46 mounted 3 webhook routers; passes 47-48 confirmed clean; pass 49 wired Download Report PDF on EngineDashboard; pass 50 confirmed clean; pass 51 fixed bugfix-streaming-notification test; pass 52 verified; pass 53 applied weight_presets migration + SCUI engine dedup + docs/ENV_SETUP.md; pass 54 closed the *reachability* gap — AI Agents + Code Chat added to nav, Code Chat GitHub tab + embaImport + env docs + 11 regression tests; passes 55-56 confirmed 2 consecutive clean; pass 57 ran a doc-sync audit in parallel with a deep feature audit; **pass 58 closed the end-to-end *usability* gap flagged by the user**: (a) the Learning section now has real consumer UIs — `LearningTrackDetail` (chapter reader), `LearningFlashcardStudy` (SRS-wired flip cards), `LearningQuizRunner` (multi-choice with explanations) — replacing the three stub routes that previously all redirected to LearningHome, so the 366+ definitions + chapters + questions + flashcards imported from mwpenn94/emba_modules can finally be studied; (b) AgentManager cards now show a `<AgentRecentRuns />` expansion panel backed by `openClaw.listActions` + per-run rows in `agent_actions`, plus `executeAgent` now persists run rows + increments `totalActions` / `totalCostUsd` so the "0 runs, $0.00" counter finally moves; (c) Code Chat roadmap is now persisted to `.stewardly/roadmap.json` via a `setRoadmap()` helper with a corrupted-file fallback, so admin-added items survive restarts instead of vanishing on every deploy. 3 new unit tests lock in the roadmap round-trip behavior. Pass 59 verification sweep — TS clean, build clean, 0 regressions. Pass 60 synced 6 docs. Pass 61 added the `workflow_instances` table (drizzle migration 0011) + 3 tRPC procs + Workflows.tsx cross-session persistence + 5 regression tests so in-progress workflow runs survive browser refresh. Passes 62-63 confirmed 2 consecutive clean. Pass 64 ran a parallel doc-sync audit and refreshed SETUP_GUIDE + STEWARDLY_COMPREHENSIVE_GUIDE + stewardly-platform-report + INTELLIGENCE_ARCHITECTURE + CONSENSUS + MASTER_OPTIMIZATION_GUIDE to current metrics; pass 66 caught and fixed a 122-vs-123 file count drift across CLAUDE + REMAINING_ITEMS. **Pass 67 closed a second batch of stub-page gaps caught by the deep feature audit**: `/admin/system-health` was rendering a hardcoded 34-entry CRON_JOBS array with "0 warnings / 0 errors" static counters — now wired to the real `integrations.getSchedulerStatus` query (10s live refresh) with per-job `lastRun`/`lastError`/`runCount`/`errorCount` telemetry and a "Run now" button backed by `integrations.triggerSchedulerJob`; `/admin/data-freshness` was rendering 14 hardcoded PROVIDERS — now wired to `dataIngestion.listSources` + `runIngestion` + `updateSource` (pause/resume) with freshness-window traffic lights derived from real `lastRunAt` timestamps; `/admin/lead-sources`, `/admin/billing`, `/admin/api-keys` all had hardcoded data but no backend wire — now carry clear "Design preview — not live data" banners so admins aren't misled. Passes 68-69 confirmed 2 consecutive clean. **Pass 70-74 ran a THIRD wave of stub-page fixes after a follow-up audit uncovered 6 more**: `/admin/rate-management` was a stub — now fully wired to `adminIntelligence.getRateProfiles` + `getRecommendations` + `generateRecommendation` + `applyRecommendation` + `dismissRecommendation` (real tRPC, admin-gated, 30s refetch); `/leads` LeadPipeline was rendering 7 hardcoded DEMO_LEADS — now reads `leadPipeline.getPipeline` into a 7-column Kanban with drag-click status updates via `leadPipeline.updateStatus` (folding the 11 schema statuses into 7 Kanban columns); `/compliance-audit` ComplianceAudit was rendering 6 fake AUDIT_EVENTS — now reads `compliance.getReviews` + `compliance.getDashboardStats` with real severity/status derivation from `compliance_reviews` rows; `/crm-sync` got its "Sync Now" button wired to the real `crm.sync` mutation (provider + direction pickers); `/admin/team` TeamManagement and `/client-dashboard` ClientDashboard kept their mock data but gained clear "Design preview" banners pointing users at the live features that DO exist. Pass 73-74 confirmed 2 consecutive clean scans. **Pass 76 fixed two production bugs captured in screenshots**: (a) the Initial Seed button returning "0 disciplines, 0 tracks (0 skipped)" — root cause was that `seedLearningContent` silently swallowed every error inside `upsertDiscipline` / `createTrack` and the counter logic over-counted existing rows; pass 76 rewrote seed to talk to drizzle directly, distinguish inserted vs skipped, and bubble up real error messages into a new `errors[]` field that ContentStudio toast now surfaces. (b) the Import from GitHub button throwing "object is not iterable (cannot read property Symbol(Symbol.iterator))" — root cause was that `embaImport.ts` assumed `data.disciplines` was an array but the real `mwpenn94/emba_modules` JSON ships it as `{ "Accounting": { color, abbr, icon }, ... }`; pass 76 rewrote the types to match the real JSON shape (object-keyed disciplines, tracks use `key` not `slug`, questions use `correct` not `correct_index`, specializations are string[]) and iterates via `Object.entries()` with a tolerance branch for future array-shaped releases. Added 2 regression tests (7/7 total) locking in the authoritative shape. **Pass 77 unified GitHub credentials behind the Integrations system**: added a `github` provider to `seedIntegrations.ts`, built `loadGitHubCredentialsForUser(userId)` that prefers a user-scoped `integration_connections` row with env `GITHUB_TOKEN` as a fallback, updated `codeChat.githubStatus` + `githubListOpenPRs` to pass `ctx.user.id` through, and the Code Chat GitHub tab now shows "Credential source: your connected account (/integrations)" vs "deployment env var (GITHUB_TOKEN)" so admins know which identity is being used. Rewrote the GitHub section of `docs/ENV_SETUP.md` to document both paths. **Pass 78 integrated Code Chat as a mode inside the main Chat page**: added a new `codeChat.chat` tRPC procedure that runs a Claude-Code-style ReAct loop over the existing codeChat tool definitions (`read_file`, `list_directory`, `grep_search` for everyone; `write_file`, `edit_file`, `run_bash` gated on admin + allowMutations), wired a "CodeChat" mode button into the Chat.tsx mode toggle alongside Single/Loop/Consensus, added a write-mode toggle + iterations config, and the existing model picker just works for single-model or multi-model orchestration since the ReAct loop accepts `model` override. Pass 79-80 confirmed 2 consecutive clean convergence scans. Pass 81 synced doc stamps. **Pass 82 ran a UI/UX landscape audit** that caught stale prompt claims (v9 "12 nav groups" vs real state of 3 arrays + flat 19-item TOOLS_NAV) so future passes don't chase phantom targets. **Pass 83 grouped the 19-item TOOLS_NAV into 5 semantic sections** (Home / Work / Intelligence / Relationships / Learning) via a new `section` field on `NavItemDef`, added `renderSectionedTools()` in AppShell, added a visible Command Palette trigger to the sidebar (dispatches `toggle-command-palette` CustomEvent, listened to by `CommandPalette.tsx`), and fixed a silent icon-fallback bug where 5 nav items were defaulting to the Zap icon because Calculator / LayoutDashboard / Users2 / Database / Target weren't in ICON_MAP. **Pass 84 created a standalone Dashboard.tsx** under prompt v9.0 — then **Pass 85 fully reverted it** after prompt v10.0 explicitly flipped the decision ("Chat IS the landing page and feature gateway — do NOT create Dashboard.tsx"): deleted Dashboard.tsx (-479 lines), removed import + `/dashboard` route from `App.tsx`, removed nav entry from `navigation.ts`, reverted `SignIn.tsx` signIn/signUp success handlers + `isAuthenticated` redirect to `/chat`, reverted `Welcome.tsx` handleGetStarted + header button to `/chat`. Pass 83 work was preserved because it's valuable under both v9 and v10. **Pass 86 built the feature-gateway UX inside the Chat empty state** instead of a separate page: enhanced the existing `WelcomeScreen` component in `Chat.tsx` with (a) a compact 4-tile activity strip (Chats / Insights / Mastery / Due — authenticated only, hidden for guests), (b) a proactive insight banner that clicks-to-seed a contextual prompt via `onPromptClick` so users stay in chat, (c) a role-gated feature discovery card grid (Intelligence / Engine Dashboard / Learning / Lead Pipeline for advisor+ OR Calculators for user). Added 3 new tRPC queries in the parent `Chat` component — `insights.list` (limit 3), `insights.stats`, `learning.mastery.summary` — all gated on `isAuthenticated && !conversationId && retry:false` so only empty-state renders hit them. Added `WelcomeStat` helper component. All rendering-only — the Chat state machine, SSE pipeline, mode controls, and compliance disclaimer paths are untouched. Passes 87-88 confirmed 2 consecutive clean convergence (TS 0 errors, navReachability 6/6, wiring-verification 12/12, learning services 9 files / 83 tests, build 21.54s, full suite 3,103/3,215 matching baseline). **Pass 89 (after user feedback that "the chat ui looks ridiculously overwhelming" + "the learning content is still non existent"):** reverted the pass 86 WelcomeScreen stat strip + insight banner + feature card grid (these were the clutter the user flagged), and fixed the *real* learning content bug — `bootstrapLearning()` was gating the GitHub content import behind `EMBA_IMPORT_ON_BOOT=true` (opt-in), so a fresh deploy landed with 12 metadata-only tracks and zero chapters / questions / flashcards. Pass 89 flipped the gate to opt-out via a new `importNeeded()` probe that checks `learning_chapters` row count (skips on no-DB / already-imported). **Pass 90 (UI/UX recursion against the actual v10.0 prompt):** found and fixed three live bugs: (a) AppShell `navExpanded` + `adminExpanded` defaulted to `false` so first-time visitors saw "Navigate ▶ / Admin ▶" with zero items visible — flipped to default `true` (still localStorage-persisted); (b) Chat sidebar dumped all 18 nav items as a flat list, ignoring pass 83's section grouping — rewrote it to render the same 5-section grouping (Home/Work/Intelligence/Relationships/Learning) AppShell uses; (c) Chat had no visible Command Palette trigger — added the same "Search pages… ⌘K" button AppShell got in pass 83. Plus Target 3 (Chat cockpit collapse): wrapped the model picker + 4-button chat mode segment + per-mode config (loop pills, codechat config, consensus deep link) in a `{advancedOpen && (...)}` toggle. Default = collapsed. Visible state = `[+] [Focus] [More] ... [audio] [send]`. Auto-expands when user activates a non-default mode. Pure rendering-only — every state machine + SSE handler + tRPC call is byte-for-byte identical. **Pass 91:** four concrete bugs/gaps: (a) Landing.tsx flashed 196 lines of unreachable hero+feature-card UI before redirecting every visitor to /chat — stripped to a 30-line pure redirect; (b) chat empty state Onboarding always showed "Professional Setup" even for casual `user` role — now picks `client_onboarding` for users and `professional_onboarding` for advisor+; (c) created `client/src/components/HonestPlaceholder.tsx` with `{willDo, needed, workingAlternative}` props per Target 5, replaced 5 inline "Design preview" banner duplicates across BillingPage / APIKeys / TeamManagement / AdminLeadSources / ClientDashboard; (d) Target 7 / WCAG 2.4.1 — added a visually-hidden "Skip to main content" link as the first focusable element in BOTH AppShell and Chat with matching `<main id>` + `tabIndex={-1}` targets. **Pass 92 (Target 8 mobile):** built the v10.0 mobile bottom tab bar inside AppShell — 5 tabs (Chat / Tools / Insights / Learn / Menu), 44px+ touch targets per WCAG 2.5.5, `aria-current="page"` on the active tab, hidden on lg+ where the persistent sidebar is visible. Main content gets `pb-16 lg:pb-0` so the last row isn't covered. Chat.tsx is intentionally NOT touched because its sticky-bottom input bar would conflict. **Pass 93 (Target 2 done minimally):** added back ONE thing — a single proactive insight banner that ONLY renders when `proactive_insights` has ≥1 new high-priority row. No stat tiles, no feature cards, no "Or explore a workspace" header. First-time users see exactly the same clean empty state as before; returning users with a portfolio drift / life event / compliance insight see one amber banner above the suggestion chips that seeds a contextual prompt on click. Passes 94-95 confirmed 2 consecutive clean convergence. **Pass 96-102 visual transformation under prompt v11.0 ("delight is the mandate, don't preserve mediocrity"):** Pass 96 landscape + pass 97 exploration generated 3 visual direction branches (Stewardship Gold / Midnight Emerald / Warm Neutrals), converged on **Branch A Stewardship Gold** (deep navy + warm gold #D4A843, DM Serif Display + Plus Jakarta Sans). Pass 98 implemented the tokens in `client/src/index.css`: `--primary`/`--accent`/`--ring`/sidebar tokens → `oklch(0.76 0.14 80)` gold; `--chart-1` promoted to gold, sky demoted to `--chart-3`; `--foreground` hue shifted from 255 (cold) to 80 (warm cream); `--font-sans` → Plus Jakarta Sans; `--font-heading` → DM Serif Display. Added delightful `:focus-visible` ring (double ring + 12px glow halo replacing the browser's ugly blue outline), `@media (prefers-reduced-motion: reduce)` override (WCAG 2.3.3 compliance disabling pulse glow / message-in / avatar-talking), and new `.card-lift` utility (hover translateY + gold shadow halo). Pass 99 added an `<div role="status" aria-live="polite">` inside Chat main for screen reader streaming announcements + fixed the stale `g+d → /dashboard` shortcut (Dashboard.tsx deleted in pass 85) to `g+i → /intelligence-hub`. Pass 100 rewired Welcome.tsx (19 hardcoded sky/slate refs) + SignIn.tsx (5 refs) — background mesh glows, logo, CTAs, hero highlight, feature cards all flipped to semantic tokens (`bg-accent`, `text-accent`, `bg-chart-2`, etc) + `font-heading` on headings. Pass 101 mass-swept 18 more files across pages and global components (AdminSystemHealth, CRMSync, ModelResults, OrgBrandingEditor, OrgLanding, Organizations, PartGPages, Portal, Products, ProfessionalDirectory, ProficiencyDashboard, AIOnboardingWidget, AnalyticsDashboard, CaptureModal, ChangelogBell, ConsentBanner, InlineChart, LiveSession, NotificationBell) via perl replace mapping sky-* Tailwind utilities → accent semantic tokens. InlineChart.tsx chart.js palette reordered to lead with gold rgba(212,168,67,0.8) matching `--chart-1`. OrgLanding + OrgBrandingEditor backgrounds swapped from `from-slate-900 via-slate-800 to-slate-900` to `bg-background`. Passes 101-102 confirmed 2 consecutive clean convergence (TS 0 errors, navReachability 6/6, wiring-verification 12/12, learning services 9 files / 83 tests, full suite 3,103/3,215 matching baseline, build clean in 19.68s). Final state: 3,103 passing across 109 files in local dev, 123/3,215 full suite, 0 regressions, 0 TS errors, 0 TODOs. Visual identity: **Stewardship Gold** (warm gold + deep navy, DM Serif Display + Plus Jakarta Sans). **Pass 120 implemented the 24-component Claude AI design optimization** (Passes 81-120): added Platform Intelligence Layer (PILProvider + FeedbackDispatcher + feedbackSpecs — multimodal intent processing, voice navigation, hands-free mode), AudioCompanion (persistent TTS player with Web Speech fallback + server-side Edge TTS at /api/tts), PersonaSidebar5 (5-layer Person→Client→Advisor→Manager→Steward nav replacing the old 3-array sidebar, with conversation grouping, mobile drawer auto-close, ARIA roles), ChatGreetingV2 (role-specific suggestion chips with daily rotation, feature discovery cards, AI health banner), MobileChatLayout (mobile-first chat with bottom-sheet options), NewLanding (public marketing page at /welcome-landing with animated hero, capability cards, trust signals), MyWork (aggregated work dashboard at /my-work), MyFinancialTwin (client financial twin at /financial-twin with risk profile, goals, insights, privacy controls), CodeChatSetup (3-step setup wizard), OnboardingFlow (per-persona first-run flow), ExamSimulator (4-mode practice exam engine with timer pause on hidden tab), DisciplineDeepDive (tabbed definitions/formulas/cases/FS learning), CaseStudySimulator (branching scenario decisions with compliance flags), AchievementSystem (gamification with streaks, goals, milestones), ConnectionMap (d3-force concept relationship graph), AudioPreferences (voice/speed/pitch/content style settings at /settings/audio), CelebrationEngine (canvas particle system), TypingIndicator, EmptyStates, stewardErrors. Database: 3 new audio tables (audio_scripts, user_audio_preferences, user_audio_overrides) via migration 0012. CSS: feedback color tokens, timing tokens, message/card/skeleton/mic-pulse animations, safe-bottom areas, reduced motion. All integrated into App.tsx routing. TS 0 errors, 3,102/3,215 test suite, 0 new regressions. **Pass 136 (external QA):** wired missing P125-127 code (audioRouter, clientRouter, ChatGreetingV2, PersonaSidebar5, OnboardingFlow default case), dead code removal (481 lines from Chat.tsx WelcomeScreen), bundle optimization (manualChunks in vite.config.ts), brand consistency (stewardry→stewardly storage key, STEWART→STEWARD typos). **Pass 137 ran a 5-track recursive QA audit** and fixed: (P0) renamed `client` router → `clientPortal` to eliminate tRPC built-in `.client` collision that broke all type inference across the app; (P0) rewrote `client.ts` to await `getDb()`, use numeric `userId`, replace non-existent `suitabilityResponses` import with `suitabilityAssessments`; (P0) fixed `audio.ts` — all 4 `getDb()` calls now awaited + null-guarded via `requireDb()`; (P0) added missing `getNavIcon()` to Chat.tsx that was undefined after dead code removal; (P0) fixed robots.txt sitemap URL from `stewardry.manus.space` → `stewardly.manus.space`; (P1) added `aria-label` attributes to 30+ icon-only buttons across 25 pages/components for WCAG 2.1 Level A compliance; (P1) converted IncomeStreamBreakdown clickable `<div>` → semantic `<button>` with keyboard support; (P1) standardized "log in" → "sign in" terminology across 5 files; (P1) added `role="presentation"` to ContextualHelp backdrop overlay. **Pass 138** convergence scan caught 3 remaining send/stop/loading buttons in ChatInputBar + Chat.tsx without aria-labels — fixed. **Pass 139** second consecutive convergence verification — 0 TS errors, build 23.57s, 3,103/3,215 tests, 0 regressions, 0 new P0/P1 items across all 5 QA tracks. **Passes 138-139 declared convergence; passes 140-144 continued:** Pass 140 fixed P0 TTS security (publicProcedure → protectedProcedure on speak/voices, voice default unified to en-US-GuyNeural) + P1 AudioCompanion aria-labels (11 buttons). Pass 142 fixed PersonaSidebar5 title → aria-label on 2 icon buttons. Passes 143-144 confirmed 2 consecutive clean convergence — 0 TS errors, build 19.45s, 3,103/3,215 tests, 0 new P0/P1 items. **Pass 145 unified the Chat sidebar navigation with the 5 persona layers** (Person/Client/Advisor/Manager/Steward) matching PersonaSidebar5, replacing the old Home/Work/Intelligence/Relationships/Learning section grouping that created inconsistent navigation between AppShell and Chat pages; removed the old NAV_ICON_MAP/getNavIcon/toolsNav/adminNav code (-87 lines net); hid the advanced chat controls (More/Less toggle, streaming toggle, mode badge) on mobile via `hidden md:flex` breakpoints so mobile users see only `[+] [Focus] [Audio] [Send]`. Passes 146-147 confirmed 2 consecutive clean convergence. **Pass 148 fixed 4 broken sidebar nav paths** that caused 404s when clicked: `/progress` → `/proficiency`, `/recommendations` → `/insights`, `/wealth-engine` → `/calculators`, removed phantom `/manager/performance`; fixed Stewardship Gold non-compliance in MyWork.tsx (`text-sky-400` → `text-chart-3`) and AchievementSystem.tsx (5 hardcoded category colors → `chart-1..5` tokens). Pass 149 fixed last remaining `text-sky-400` in AchievementSystem. Passes 150-151 confirmed 2 consecutive clean convergence. **Pass 152 fixed 3 sidebar nav items pointing to redirect URLs** instead of actual destinations: Documents `/documents` → `/settings/knowledge`, Insights `/insights` → `/intelligence-hub`, Suitability `/suitability` → `/settings/suitability`; fixed IntelligenceFeed.tsx `text-slate-400` → `text-muted-foreground`. Passes 153-154 confirmed 2 consecutive clean convergence. **Pass 155 restored UX delight regressions**: ChatGreetingV2 feature cards now have card-lift hover effect; CelebrationEngine wired into LearningQuizRunner + LearningFlashcardStudy (heavy particles on 100%, medium on 80%+); CaseStudySimulator added to LearningHome Learning Tools grid (was routed but had no entry point). Passes 156-159 confirmed 4 consecutive clean convergence. **Pass 177 fixed 5 bugs found by fresh independent audit**: (P0) ExamSimulator finishExam() gave credit for unsubmitted selected answer — removed bonus count; (P1) Chat.tsx setTimeout not cleaned up on unmount — added clearTimeout; (P1) Chat.tsx loopPollRef interval not cleaned up on unmount — added useEffect cleanup; (P1) PersonaSidebar5 LEARN_ITEM match patterns included stale routes — updated to real App.tsx sub-routes; (P2) AudioCompanion progress bar aria-valuetext added. **Pass 178** guarded voice.start() with try-catch for Web Speech API safety. **Pass 183** removed duplicate @keyframes message-in + added missing card-flip utility classes. **Pass 184** guarded unprotected JSON.parse in NotificationsTab + Workflows that could crash on corrupted localStorage. Passes 185-200 confirmed 16 consecutive clean convergence — 0 TS errors, build 21.16s, 3,103/3,215 tests. **Post-convergence enhancement passes continued through Pass 200+**: Pre-existing fixes (Dockerfile path, 18 dead imports removed, ThemeContext memoized, ErrorBoundary prod-safe, console.log removed, package.json stewardly-ai, unused CSS removed, dead audioScriptGenerator service removed). Infrastructure enhancements (autonomous processing saturation detection via Jaccard overlap, consensus agreement_scored event with numeric confidence, memory conflict resolution + temporal weighting, 5-layer model preference routing, AudioPreferences tRPC persistence, SRS daily due-review reminder cron, weekly EMBA auto-import cron, URL hallucination guardrail wired into chat pipeline, memory.stored event bus emission, proactive insights auto-generation on compliance.flagged, enhanced tool status messages). Security fixes (adminProcedure UNAUTHORIZED for unauthed, updateTrack canPublish RBAC guard). UX fixes (Wealth Engine 5-page access restored on Calculators, 6 standalone planning pages linked, Code Chat moved to Person layer for all users, processing modes consolidated into Focus menu for mobile, mobile double-header eliminated via hidden lg:block on page headers, 75+ pages with AppShell, all pages titled). **Post-convergence Code Chat cloud parity**: Added SSE streaming endpoint (`POST /api/codechat/stream`) for real-time tool execution visualization — users see each file read, grep search, and edit as it happens during ReAct loop execution. Built `useCodeChatStream` React hook consuming SSE events with `ToolEvent` state management. CodeChat.tsx now uses streaming with live tool event display, abort button, and inline editing support (admin write mode). **Passes 232-235 (Code Chat conversation controls + message-level interactions)**: Pass 232 added the `/compact` slash command — `conversationCompact.ts` pure `compactConversation(messages, keepRecent)` collapses the older window into a single synthetic assistant message containing a markdown summary (user prompts bulleted, aggregate tool-call counts via `summarizeToolEvents`, distinct files touched, total duration) stamped with `model: "[compacted]"` so the UI shows "compacted" in the meta bar. `buildSummary` is split out for easy test locking. The slash command registry grew a `{ compact: true, keepRecent? }` result shape that `handleSend` detects and routes through `loadMessages(compacted.messages)`. Claude Code's terminal `/compact` convention with no LLM round-trip. Pass 233 added per-message bookmarks — `messageAnnotations.ts` pure `toggleBookmark`/`isBookmarked` with 500-entry cap, localStorage persistence; a **Star** button in each assistant message action bar fills on toggle, a config-bar Bookmarks button opens a modal listing every bookmarked message in the current session with click-to-scroll (uses `data-message-id` attributes + a brief amber highlight pulse on jump). Pass 234 added a toggleable message outline rail — `Outline` button in the config bar flips a 240px left column showing every user turn with "turn N" numbering and a 80-char preview; clicking any entry smooth-scrolls to the corresponding message with the same highlight pulse. Pure UI against the existing `messages` array. Pass 235 added thumbs-up/thumbs-down reactions — `setReaction` pure function handles toggle-off-on-same-kind + replace-on-opposite semantics; reactions persist to `localStorage['stewardly-codechat-reactions']` as a `{messageId → "up"|"down"}` map; action bar now has thumbs-up/down buttons colored emerald/red when active. `countReactions` helper for future stats dashboards. New tests: 12 compactConversation + 19 messageAnnotations + 3 new slashCommand `/compact` branch tests — 34 additional tests pushing the codeChat surface to **402 passing across 19 files**. TS clean, 0 regressions. **Pass 236 (Plan Mode — Claude Code parity)**: Added Plan Mode so the agent can generate a step-by-step plan before executing. `client/src/components/codeChat/planMode.ts` is a pure-function plan state machine: `parsePlanFromText` handles numbered lists (`1. foo`), `Step N:` prefixes, AND bullet fallbacks, with regex-based title extraction from markdown headings and tool-hint detection (`[tool: read_file]` or `(uses grep_search)`); `planProgress` derives done/failed/skipped/executing counts + completion pct from step status; `updateStepStatus`/`updateStepDescription`/`addStep`/`removeStep`/`reorderStep`/`approveAllSteps`/`rejectPlan` are all pure immutable mutations that return a new Plan; `buildExecutionPrompt` formats an approved plan into the execution prompt the ReAct loop runs against (with the `✓ Step N complete` / `✗ Step N failed` / `— Step N skipped` progress marker convention); `snapshotPlan`/`restorePlan` handle localStorage serialization. `PlanReviewPanel.tsx` is the interactive UI — a Claude-Code-style review card rendered inline in the assistant message that offers inline description editing, ↑/↓ reordering, add/remove step controls, per-step status icons, a live progress bar once execution begins, and an Approve & Execute action bar; draft mode allows full editing, post-approval mode is read-only with live status updates. Added `/plan <task>` (aliased to `/p`) slash command that rewrites input into a plan-generation prompt (with explicit "do NOT execute" instructions so the LLM outputs structure only) and sets a `planExpecting` flag that auto-parses the next assistant response via `parsePlanFromText` into a `Plan` attached to the message id. Plans persist to `localStorage['stewardly-codechat-plans']` as a message-id → PlanSnapshot map so they survive refresh. `handleExecutePlan` wraps the ReAct loop call, auto-expanding maxIterations to match step count when the plan is longer. New tests: 42 planMode (parser, mutations, progress, execution prompt, snapshot round-trip) + 3 slashCommand `/plan` branch tests — 45 additional tests pushing the codeChat surface to **447 passing across 16 files**. TS clean, 0 regressions, build 22.68s. **Pass 237 (Live agent todo tracker — Claude Code TodoWrite parity)**: Added a mid-execution progress tracker so the agent can stream live todos to the UI as it works through a multi-step task, mirroring Claude Code's TodoWrite tool. Server-side added `update_todos` to `CodeToolName` and `CODE_CHAT_TOOL_DEFINITIONS` in `codeChatExecutor.ts` — a no-op tool whose dispatcher validates + normalizes an `AgentTodoItem[]` payload (content/activeForm/status with pending|in_progress|completed, auto-id, 50-item cap, drops empty content, defaults invalid statuses to pending) and returns a `todos` kind result. Added `update_todos` to the SSE route's `READ_ONLY_TOOLS` allowlist (safe for all roles since it has no filesystem side effect) + emits a dedicated `todos_updated` SSE event alongside the normal tool_start/tool_result so the client can render a live panel without waiting for the final response. Updated the server system prompt to tell the agent to call `code_update_todos` early on any 3+ step task. Client-side added `client/src/components/codeChat/agentTodos.ts` (pure state helpers: `parseTodosPayload` normalizes + defensively validates SSE payloads with the same rules as the server; `mergeTodoList` treats the new list as authoritative with id-matched merge; `todoProgress` derives pending/inProgress/completed/pct/allDone; `currentTodo` finds the active item for the summary strip; localStorage persistence per-message). `AgentTodoPanel.tsx` is the presentational component with two variants — `live` (compact sticky strip with progress bar + current activeForm) during execution and `card` (full interactive checklist) below finished messages. `useCodeChatStream.ts` hoists `currentTodos` state, listens for `todos_updated` events, merges with the running list, captures the final snapshot on `done` into `msg.agentTodos`, and exposes the live state to the UI. Wired `update_todos` into the `ToolPermissionsPopover` as a non-mutation default and flipped the tool count badge from `N/6` to `N/7`. New tests: 20 agentTodos (parseTodosPayload defensive validation, mergeTodoList semantics, todoProgress derivation, currentTodo selection) + 4 server-side dispatcher tests (normalize, cap, BAD_ARGS, default status) — 24 additional tests pushing the codeChat surface to **452 passing across 20 files**. TS clean, 0 regressions, build 22.05s. **Pass 238 (CLAUDE.md auto-loading — Claude Code project instructions parity)**: Added automatic discovery + injection of project instruction files into the Code Chat system prompt, mirroring Claude Code's `CLAUDE.md` auto-loader behavior. `server/services/codeChat/projectInstructions.ts` walks the workspace root for three conventions in priority order — `.stewardly/instructions.md` (Stewardly-specific override), `CLAUDE.md` (Claude Code convention), `AGENTS.md` (alternative agent convention) — each capped at 32KB with a UTF-8 byte-length hard-cap so the content mtimestamp-keyed cache doesn't blow the context budget. `loadProjectInstructions` returns entries in priority order; `buildInstructionsPromptOverlay` formats them into a markdown-structured system-prompt block with per-file headers + truncation markers; `manifestForUI` strips the full content and returns a 512-char preview for the popover; `clearProjectInstructionsCache` invalidates the in-process cache for the reload button. The SSE stream route calls `loadProjectInstructions(WORKSPACE_ROOT)` when `includeProjectInstructions: true` (default), appends the overlay to the system prompt, and emits a dedicated `instructions_loaded` SSE event with the file list so the client can show a loaded-count badge. Two new tRPC procedures: `codeChat.projectInstructions` (query — returns the manifest) and `codeChat.reloadProjectInstructions` (mutation — clears cache + reloads). Client-side `ProjectInstructionsPopover.tsx` is a modal showing every auto-loaded file with kind badge (stewardly/claude/agents), byte-length + truncation warning, 512-char preview, reload button, and a master toggle that persists to localStorage. `useCodeChatStream.ts` now tracks `loadedInstructionFiles` state and forwards `includeProjectInstructions` through the SSE POST body. CodeChat.tsx config bar grew a `Rules` button that lights up accent-gold when any files are loaded and falls to a strikethrough disabled state when the toggle is off. Every `sendMessage`/`regenerateLast`/`handleExecutePlan` call now passes the user's current toggle. New tests: 15 projectInstructions (workspace walk, priority order, directory-skip, size truncation, mtime-based cache hit/miss, manifestForUI) — pushing the codeChat surface to **467 passing across 21 files**. TS clean, 0 regressions, build 21.94s. **Pass 239 (Edit undo/redo history ring buffer)**: Added a per-session edit history ring buffer that captures every file write/edit — from agent ReAct tool calls AND manual FileBrowser inline saves — and lets users undo/redo with Ctrl+Z / Ctrl+Shift+Z. `client/src/components/codeChat/editHistory.ts` is a pure state machine: `EditHistoryState` holds entries + cursor; `recordEdit` appends (dropping forward history after an undo, no-op on before===after, auto-id/timestamp); `undo`/`redo` return `{state, entry}` tuples so the caller can restore file content; `canUndo`/`canRedo` derive from cursor position; `dropEntry` drops a specific entry and adjusts cursor; `clearHistory` resets to empty; 50-entry ring cap; `serializeHistory`/`parseHistory` handle localStorage with defensive validation that filters malformed entries and clamps cursor to valid range. `EditHistoryPopover.tsx` is the inspection UI — shows entries newest-first with per-entry path/origin/kind badges, byte-length delta (`+123B`/`−45B`), relative timestamp, undo/redo/clear action bar, cursor indicator (current entry highlighted accent-gold, redo-able entries faded), and a "Revert to before" button per entry that skips the cursor and writes the entry's `before` content directly. Integration: `CodeChat.tsx` watches completed assistant messages for `edit_file`/`write_file` tool events with JSON before/after snapshots (Pass 205 added these), extracts them into history with `origin: "agent"`; `FileBrowser.onSave` dispatches a `codechat-edit-recorded` window event that CodeChatInterface listens for to record manual saves with `origin: "manual"`; Ctrl+Z / Ctrl+Shift+Z keyboard handlers call `handleUndoEdit`/`handleRedoEdit` (only outside text fields so native in-textarea undo still works); these handlers call the `codeChat.dispatch` mutation with `write_file` to restore the content and update the cursor. Config bar grew a `History` button that only renders when there are entries (shows `N/M applied` badge) + opens the popover. `KeyboardShortcutsOverlay` documents the new shortcuts. New tests: 22 editHistory (recordEdit forward-history dropping, MAX_ENTRIES cap, undo/redo round-trip, cursor clamping, serialize/parse defensive parsing, summarizeHistory) — pushing the codeChat surface to **376 passing across 17 files**. TS clean, 0 regressions, build 22.51s. **Pass 240 (Scratchpad side drawer)**: Added a persistent per-device notes buffer — a long-lived free-form notepad that lives outside any single conversation so users can dump commands, file paths, exploratory prompts, or multi-step instructions they'll feed to the agent piece by piece. `scratchpad.ts` is a pure module: `ScratchpadState` = `{content, updatedAt}`; `setContent` clamps at 200KB; `append` adds newline separator if prior doesn't end with one (no-op on empty input); `prepend` mirrors append from the top; `insertAt` supports cursor-position insertion with range clamping; `scratchpadStats` derives chars/words/lines/pct; `extractLines` pulls a 1-indexed inclusive line range (clamped to valid bounds); `parseScratchpad` handles malformed JSON and rejects non-string content; localStorage persistence auto-saves on every keystroke via a mirror `useEffect`. `ScratchpadPanel.tsx` is a side drawer (hidden md:flex, desktop only) with header badge showing chars/words/lines, flex-1 textarea, footer action bar with Send all / Send selection / First line / Copy / Download / Clear, and a bottom capacity meter that turns amber at 50% and red at 80% of the 200KB cap. Send actions call `onInsertIntoPrompt(text)` which appends to the chat input with smart newline spacing so in-progress drafts aren't clobbered. Selection tracking uses the textarea's onSelect + selectionStart/selectionEnd so "Send selection" works against the live caret range. Integration: `CodeChat.tsx` loads/saves scratchpad state, adds a `Notes` button to the config bar that lights up accent-gold when content exists, toggles the drawer inline next to the optional Files panel. New tests: 27 scratchpad (setContent clamp, append/prepend smart spacing, insertAt clamp, stats derivation, extractLines range clamping, defensive parse with non-string content, malformed JSON fallback) — pushing the codeChat surface to **403 passing across 18 files**. TS clean, 0 regressions, build 22.29s. **Pass 241 (Agent memory — persistent facts across sessions)**: Added long-lived agent memory — a per-device store of facts the agent reads at the start of every session, mirroring Claude Code's long-term memory feature. `client/src/components/codeChat/agentMemory.ts` is a pure store: `MemoryEntry` has `{id, content, category, createdAt, updatedAt}`, four categories (`project`/`preference`/`fact`/`warning`) with emoji icons; `addMemory` dedupes on exact content+category match (moves existing entry to top with bumped updatedAt), clamps content at 1000 chars, caps at 200 entries with newest-first ordering; `removeMemory`/`updateMemory`/`clearMemory` are immutable; `filterByCategory` narrows by category or returns all; `summarizeMemory` derives total + per-category counts + most-recent entry for the config bar badge; `buildMemoryOverlay` formats entries into a grouped markdown block (`## PROJECT`/`## PREFERENCE`/…) with the canonical category order for the system prompt; `parseMemory` handles malformed JSON, rejects non-string content, filters unknown categories, clamps oversized content, and caps entries on load. `AgentMemoryPopover.tsx` is a modal with category filter chips (pill counts by category), an add form with textarea + category picker (⌘+Enter to save), editable entry list with inline textarea + category picker + save/cancel, per-entry edit/delete buttons, and a clear-all confirm at the bottom. Server-side `codeChatStream.ts` accepts an optional `memoryOverlay` string in the POST body and appends it to the system prompt (hard-capped at 8KB for safety) so the agent sees the memory alongside the project instructions. Added a `/remember <fact>` slash command (aliased `/mem`) that dispatches a `codechat-remember` window event — the CodeChat listener catches it and adds to memory with `category: "fact"`. Config bar grew a `Brain` button that shows the entry count when non-empty and opens the popover. Every `sendMessage`/`regenerateLast`/`handleExecutePlan` call serializes the current memory overlay via `useMemo(buildMemoryOverlay)` and passes it through. New tests: 30 agentMemory (add/dedupe, content clamping, MAX_ENTRIES drop-oldest, remove, update, filter, buildMemoryOverlay category ordering, summarize, defensive parser) + 3 slashCommand `/remember` branch tests — 33 additional tests pushing the codeChat surface to **436 passing across 19 files**. TS clean, 0 regressions, build 21.90s. **Pass 251 (TypeScript diagnostics panel — LSP parity): Added a diagnostics inspector that shells out to tsc --noEmit and renders structured errors/warnings with click-to-jump navigation, matching Claude Code IDE extension LSP integration. `server/services/codeChat/diagnostics.ts` is a pure parser: parseTscOutput walks `path(line,col): severity TScode: message` headers via DIAG_HEADER_REGEX, attaches continuation lines (multi-line type mismatches) as details[], handles CRLF + Windows drive letters, and resets the details window on blank lines. summarizeDiagnostics derives errors/warnings/info counts + fileCount + topFiles ranked by diagnostic count; filterDiagnostics supports severity + pathPrefix + search (matches message/code/path/details); groupByFile sorts groups with most-errors-first. `diagnosticsRunner.ts` spawns `npx tsc --noEmit --pretty false` with a 60s hard timeout, 5MB stdout cap, cache TTL=30s, and in-flight dedup so concurrent callers share a single compile; non-zero exit with parseable diagnostics is NOT treated as failure (errors are expected output), but stderr with no parseable stdout becomes a fatalError. Two new tRPC procedures: `getTsDiagnostics` (query with severity/pathPrefix/search/limit/force filters, returns {diagnostics, summary, groups, cached, durationMs, fatalError}) and `rebuildTsDiagnostics` (mutation that clears cache + forces a fresh run). DiagnosticsPanel.tsx is the client UI: search input + severity filter chips + expand-all/collapse-all + Run tsc button, summary strip with colored error/warning/info badges, per-file collapsible groups with error/warning count badges, per-diagnostic click-to-jump rows that dispatch codechat-open-file with line+col, inline rendering of detail continuation lines. Wired into CodeChatPage as a new Diagnostics tab next to TODOs + registered in the action palette as tab:diagnostics with tsc/errors/typescript keywords. New tests: 20 diagnostics (parseTscOutput per-format with single/warning/continuation/blank-line-reset/multi-file/Windows-paths/CRLF/bare-header-filter/message-preservation, summarizeDiagnostics counts + topFiles ordering, filterDiagnostics per-mode + composed + search-against-details, groupByFile per-file ordering). Full server codeChat surface: 254 passing across 11 files. TS clean, 0 regressions. **Pass 250 (ANSI color parser for bash output — terminal parity): Added a pure-function ANSI SGR escape sequence parser so run_bash tool output renders with real terminal colors — green passes, red errors, dim hints, bold status lines, 256-color palette, 24-bit truecolor. `ansiParser.ts` is the pure module: parseAnsi walks the input with a CSI regex (ESC[...m) and splits into AnsiSegment[] with {text, style}; applySgrParams mutates an AnsiStyle through every canonical SGR code (0 reset, 1/2/3/4/7 attributes, 22/23/24/27 clears, 30-37 standard fg, 90-97 bright fg, 40-47 bg, 100-107 bright bg, 38;5;N and 48;5;N palette, 38;2;R;G;B and 48;2;R;G;B truecolor, 39/49 defaults); paletteToHex expands the 256-color palette (standard 0-15 + 6x6x6 cube 16-231 + 24-step grayscale 232-255); styleToCss converts AnsiStyle into a React inline style bag with proper inverse-flag fg/bg swapping; stripAnsi and hasAnsi utility helpers for the fast-path render. Non-SGR CSI sequences (cursor movement, clear) are silently consumed so the output stays clean. AnsiOutput.tsx is the presentational component: short-circuits to plain <pre> when hasAnsi returns false, otherwise maps segments to styled <span> with truncation at maxChars. TraceView in CodeChat.tsx now detects run_bash tool results via extractBashResult (parses kind="bash" results into {command, stdout, stderr, exitCode}), renders the command with a "$" prompt line, routes stdout + stderr through AnsiOutput with a bordered stderr block, and shows a per-bash "exit N" badge in the header (emerald for 0, destructive otherwise). Non-bash observations also route through AnsiOutput when hasAnsi detects escape codes (so rg --color=always, tsc with --pretty, etc. colorize correctly). New tests: 45 ansiParser (stripAnsi/hasAnsi, parseAnsi per-mode with 16-color/bright/palette/truecolor/bg/defaults/multi-segment/persistent-style/non-SGR-strip/empty-CSI-reset, applySgrParams mutation semantics, paletteToHex color cube + gray ramp, colorToCss per kind, styleToCss inverse-swap + defaults). codeChat client surface: 566 passing across 23 files. TS clean, 0 regressions. **Pass 249 (Tool audit rules + trail — PreToolUse/PostToolUse hook parity): Added a pattern-matching audit engine that evaluates every Code Chat tool call against a user-defined rule list, mirroring Claude Code hook system. `toolAudit.ts` is a pure store with AuditRule {id, label, tool, argPattern?, verdict, note?}, 4 verdicts (safe/log/warn/block), evaluateTool matching by exact-or-wildcard tool name + case-insensitive regex over JSON.stringify(args), strongestVerdict reducer, MAX_RULES=100/MAX_ENTRIES=500 caps, immutable addRule/removeRule/toggleRule/updateRule, parseState that defensively rejects invalid verdicts and ALWAYS re-seeds BUILT_IN_RULES so the safety net survives clears, exportAuditCsv producing RFC 4180 CSV for SIEM archives. BUILT_IN_RULES is a 7-entry starter set: rm -rf /|~|* → block, sudo → warn, git reset/clean -f → warn, git push --force → block, .env write → warn, DROP TABLE → warn, write_file → log. ToolAuditPopover.tsx is a two-tab modal — Rules tab with inline add/edit/toggle/delete + 4 verdict chips, Trail tab with verdict filter chips, CSV export, chronological entries showing verdict badge + tool + rule label + trimmed args preview. CodeChat.tsx watches currentTools + messages[].toolEvents via a deduped stepIndex set and evaluates every completed tool call, toasting warn/block verdicts. Config bar Shield button flips to amber with flagged count. Wired into the action palette as open:audit. New tests: 44 toolAudit (compilePattern, evaluateTool per-mode, strongestVerdict ranking, rule mutations, MAX caps, recordMatches ring buffer, parseState defensive parsing, exportAuditCsv RFC 4180 escaping, all 7 built-in rule matchers). codeChat client surface: 521 passing across 22 files. TS clean, 0 regressions. **Pass 248 (Action palette — unified ⌘K launcher)**: Added a single fuzzy-searchable command palette that exposes every UI action across the Code Chat — opening tabs, popovers, running slash commands, triggering workspace actions — through one ⌘K/Ctrl+K keyboard shortcut, matching Claude Code and VS Code's command palette convention. `actionPalette.ts` is a pure registry + scorer: `PaletteAction` has `{id, label, category, hint?, keywords?}` with 5 categories (tab / popover / slash / shortcut / workspace); `scoreAction` implements a 5-tier fuzzy ranking (exact > prefix > keyword prefix > substring > keyword substring > subsequence) and is case-insensitive; `filterActions` returns top-N sorted by score then alphabetically; `groupByCategory` renders results grouped when the query is empty; `DEFAULT_ACTIONS` is a comprehensive baseline covering all 10 tabs, all 12 popovers, 2 workspace actions, and 6 slash commands. `ActionPalettePopover.tsx` is the modal UI — fuzzy search input with auto-focus + Esc to close, grouped results when empty (with uppercase category headers) and flat ranked list when searching, arrow-key navigation with Enter to run, click-to-run with hover-follows-cursor so mouse + keyboard stay in sync, per-row hint badge showing keyboard shortcut if any. Integration: `CodeChatPage` owns the palette state, Ctrl+K handler toggles it, `handlePaletteRun` dispatches the selected action by category prefix — `tab:*` calls `setActiveTab` directly, `open:*` dispatches a `codechat-palette-open` window event that `CodeChatInterface` listens for to open the corresponding popover, `slash:*` dispatches a `codechat-palette-slash` event that pre-fills the chat input, `workspace:*` dispatches a `codechat-palette-action` event for `clear`/`abort`. This decoupled event-based bridge keeps the palette at the outer page level without coupling to the inner component's state. `KeyboardShortcutsOverlay` documents the new ⌘K shortcut. New tests: 21 actionPalette (scoreAction 5-tier ranking, case insensitivity, subsequence fallback, filterActions empty-query/limit/match, groupByCategory canonical order + preservation, DEFAULT_ACTIONS integrity with unique ids) — pushing the codeChat client surface to **477 passing across 21 files**. TS clean, 0 regressions, build 22.29s.

**Pass 247 (Circular dependency detector)**: Added Tarjan's SCC-based cycle detection over the import graph from Pass 245 so users can surface tangled circular imports in the codebase. `server/services/codeChat/circularDeps.ts` implements classic Tarjan's strongly-connected-components with per-node `indices`/`lowlinks`/`onStack`/`stack` state, returns every SCC with size > 1 as a proper multi-file cycle, and also detects single-node self-loops (a file importing itself) by checking the outgoing-edge list for a self-reference. `findCycles` runs SCC over every node that appears in either the outgoing or incoming map (nodes with zero edges can't cycle), sorts results by size descending with a stable tie-break on the first filename. `summarizeCycles` aggregates `{totalCycles, filesInCycles, largestCycle, selfLoops}`. Tarjan's (not naive DFS) because it reports each cycle exactly once even when multiple paths share nodes — the test suite locks this in with a 4-node shared-path case. One new tRPC procedure: `codeChat.findCircularDeps` returning `{cycles, summary}`. Extended `ImportGraphPanel.tsx` with a new Circular Deps section above the Hot Files leaderboard showing a summary badge (emerald "none" when clean, destructive "N cycles · M files" otherwise), per-cycle entries with size-or-self-loop badge, and clickable file list (click navigates to that file in the inspector). New tests: 12 circularDeps (acyclic, 2-cycle, 3-cycle, disjoint cycles, size-descending sort, ignoring acyclic portions, isolated nodes, complex SCC with multiple paths, summarizeCycles counts). codeChat surface: **690 passing across 30 files**. TS clean, 0 regressions, build 22.04s.

**Pass 246 (TODO/FIXME marker scanner)**: Added a project-wide marker inventory so users can see every outstanding TODO/FIXME/HACK/XXX/NOTE/BUG/OPTIMIZE/PERF comment across the workspace without a shell grep. `server/services/codeChat/todoMarkers.ts` is a pure regex scanner: `extractMarkers` walks source lines (supporting `//`, `/*`, `#`, `;`, `--`, `%`, `'`, and HTML comment prefixes), matches any of the 8 canonical marker kinds with an optional `(author)` annotation and a colon-separated message, uppercase-normalizes kinds, trims whitespace, clamps messages at 500 chars, caps at 1000 markers per file; `groupMarkers` buckets by kind/author/file; `markerSeverity` maps each kind to a 0-3 level (BUG/FIXME critical, XXX/HACK warning, TODO/OPTIMIZE/PERF normal, NOTE info); `filterMarkers` supports kind/author/pathPrefix/search filters composable in one call. `todoMarkersCache.ts` mirrors the symbol/import graph cache pattern with 60s TTL + in-flight dedup + a 25-extension allowlist covering the common languages in a polyglot codebase + a 10k global marker hard cap. Two new tRPC procedures: `codeChat.scanTodoMarkers` (query with filter params, returns `{markers, total, byKind, topAuthors, topFiles}`) and `rebuildTodoMarkers` (mutation that invalidates cache). Client-side `TodoMarkerPanel.tsx` renders a filter bar (search input + kind chips + author pill), a two-column authors/files leaderboard, and the marker list with per-row kind badge + author chip + click-to-jump via the existing `codechat-open-file` custom event. Each kind gets a dedicated icon + color (BUG/FIXME destructive, HACK/XXX amber, TODO accent, NOTE muted, OPTIMIZE/PERF chart-3). Clicking an author chip sets an active filter; click again to clear. Wired into `CodeChatPage` as a new `TODOs` tab next to `Imports`. New tests: 26 todoMarkers (extractMarkers per-syntax coverage with //, /*, #, multiple-per-file, uppercasing, author parsing, line numbers, all-kinds coverage, 1000-per-file cap, 500-char message clamp, whitespace trim; groupMarkers by kind/author/file; markerSeverity per-kind; filterMarkers kinds/author/pathPrefix/search/composed) — pushing the codeChat surface to **678 passing across 29 files**. TS clean, 0 regressions, build 22.27s.

**Pass 245 (Import/dependency graph panel)**: Added a workspace-wide import graph builder + inspector so users can answer "what does X import" and "who imports Y" without grep. `server/services/codeChat/importGraph.ts` is a pure regex-based parser that catches default/named/namespace/side-effect imports, type-only imports, and re-exports (`export * from`, `export { x } from`, `export type { T } from`), skipping comments and dynamic imports; `resolveImport` handles relative paths with `./`/`../` normalization, alias roots (`@/` → `client/src/` by default), and tries candidate extensions (`.ts`/`.tsx`/`.js`/`.jsx`/`.mjs`/`.cjs`/`.d.ts`) + `/index.*` lookups against a known-files set; `buildImportGraph` populates an `ImportGraph` with `outgoing`/`incoming`/`unresolved` maps + edge count, deduping both directions; `getFileDependencies` returns imports + importedBy + unresolved for a single file; `findLeafFiles` isolates standalone modules; `findHotFiles` ranks by inbound edge count (most-imported first); `graphStats` derives totals + avg fanout. `importGraphCache.ts` mirrors the symbol index cache pattern — 60s TTL, in-flight dedup, 256KB per file, extension allowlist. Three new tRPC procedures: `codeChat.fileDependencies`, `importGraphHotFiles`, `rebuildImportGraph`. Client-side `ImportGraphPanel.tsx` is a two-card split: left card has a path input, stats strip (files/edges/leaves/avg fanout), and a click-to-navigate hot-files leaderboard; right card shows three sections for the selected file — Imports (outgoing), Imported by (incoming), and Unresolved specifiers (bare modules that couldn't be mapped to workspace files). Clicking any file in either the imports list, importedBy list, or hot-files leaderboard switches the inspection target — full graph navigation without leaving the panel. Wired into `CodeChatPage` as a new `Imports` tab next to `Git Status`. New tests: 32 importGraph (parseImports per-shape coverage + comment skipping, resolveImport relative + parent + index + alias + bare-module + custom-root, buildImportGraph outgoing/incoming/dedupe/unresolved, findLeafFiles, findHotFiles ranking + limit, graphStats) — pushing the codeChat surface to **652 passing across 28 files**. TS clean, 0 regressions, build 22.62s.

**Pass 244 (Git status panel — workspace delta from HEAD)**: Added a live view of the Code Chat workspace's current delta from HEAD via a dedicated Git Status tab. `server/services/codeChat/gitStatus.ts` is a pure parser + subprocess runner: `parseGitStatusPorcelain` decodes `git status --porcelain=v1` output into structured `GitStatusEntry` rows with `{path, originalPath, staged, worktree, dirty}`, handling modified/added/deleted/renamed/copied/untracked/conflicted/typechange status codes plus the `orig -> new` rename arrow syntax; `summarizeGitStatus` produces per-status counts for the summary strip, classifying each entry into one primary status (prioritizing conflicts > untracked > deleted > renamed > added > modified); `runGit` spawns git with a 10s timeout, stdout/stderr capture, and SIGKILL on hang; `getWorkspaceGitStatus` / `getWorkspaceGitDiff` / `getWorkspaceGitHead` degrade gracefully (return empty on non-git workspaces so the UI doesn't break). Two new tRPC procedures: `codeChat.gitWorkspaceStatus` (returns `{entries, summary, head}`) and `codeChat.gitWorkspaceDiff` (takes `{path, staged?}`). `GitStatusPanel.tsx` is a two-card split: the left card shows branch + short sha + per-status badge strip + clickable entry list with per-row icon/color/staged-or-worktree chips; clicking an entry loads the corresponding diff via the tRPC query and renders it with syntax coloring (`+` emerald, `-` destructive, `@@` chart-3), truncated at 500 lines. Wired into `CodeChatPage` as a new `Git Status` tab next to the existing `GitHub` tab. New tests: 20 gitStatus (parser per-code coverage, rename arrow parsing, conflict dual-state, multi-line, short-line rejection, summarize prioritization) — pushing the codeChat surface to **620 passing across 27 files**. TS clean, 0 regressions, build 22.19s.

**Pass 243 (Session analytics dashboard)**: Added a cost + efficiency dashboard that rolls up the current Code Chat session's bill of materials. `sessionAnalytics.ts` is a pure module: `aggregateByModel` groups assistant messages by `msg.model`, sums input/output tokens + cost using the existing `estimateMessageUsage` helper, tracks unpriced turns separately, sorts buckets by cost descending, falls back to an "unknown" bucket for missing model names; `topExpensiveTurns` returns the top-N most costly prompts with `{messageId, promptPreview, model, usage}` sorted by costUSD so the UI can jump straight to the expensive ones; `toolUsageStats` aggregates `msg.toolEvents` into `{toolName, count, totalDurationMs, errorCount, avgDurationMs}` ordered by totalDurationMs (identifies which tools eat the most time); `bytesRatio` parses the JSON previews on `read_file`/`write_file`/`edit_file` events (same Pass 205 snapshots used by the edit-history capture) to derive `{bytesRead, bytesWritten, filesRead, filesWritten, writeRatio}` — `filesRead/Written` counted as unique path sets; `durationStats` tracks `totalMs`/`avgMs`/`spanMs` from message timestamps + `totalDurationMs`; `analyzeSession` is the top-level reducer that produces a single `SessionAnalytics` struct with user/assistant turn counts. `SessionAnalyticsPopover.tsx` is a modal with five sections: (1) totals (messages, turns, span, aggregate tokens + cost), (2) by-model breakdown with turn-count badge + cost + tokens, (3) top expensive turns (clickable to scroll via `onJumpToMessage`), (4) tool usage with count/duration/error badges, (5) bytes I/O with a read/write ratio bar (emerald read + accent write). Added a `Stats` button to the config bar (only renders when `messages.length > 0`). New tests: 20 sessionAnalytics (aggregateByModel grouping + unknown fallback + cost-descending sort, topExpensiveTurns limit/filter, toolUsageStats count + duration + error tracking + total-duration sort, bytesRatio read/write/dedup + writeRatio, durationStats span + avg, analyzeSession top-level summary) — pushing the codeChat surface to **456 passing across 20 files**. TS clean, 0 regressions, build 22.50s.

**Pass 242 (Semantic symbol navigation — VS-Code Go to Symbol parity)**: Added a workspace-wide symbol index + navigator so users and the agent can jump directly to definitions without grep. `server/services/codeChat/symbolIndex.ts` is a regex-based extractor that recognizes 8 symbol kinds (function / class / interface / type / const / let / var / enum) across TS/TSX/JS/JSX/MJS/CJS files; `extractSymbols` walks source lines with a configured pattern per kind (honoring `export`, `async`, `abstract`, `implements`, `extends`, default exports, etc.), skips deeply-nested scopes (indentation >4 spaces), rejects reserved words as false-positive captures, and returns entries with `{name, kind, path, line, snippet, exported}`; `buildSymbolIndex` aggregates across files + builds a lowercased-name reverse-lookup map; `findSymbols` implements 4-tier fuzzy ranking (exact > prefix > substring > subsequence) with export + kind boosts so exported functions/classes rank above private consts; `findExactName` returns every definition with an exact case-insensitive match; `symbolIndexStats` derives totals + per-kind + file counts for the UI strip. `symbolIndexCache.ts` provides a process-global index cache with 60s TTL + in-flight dedup so concurrent callers share a single rebuild; 256KB max file size + 6-extension allowlist prevent pathological runaways. New `find_symbol` tool in `CODE_CHAT_TOOL_DEFINITIONS` with `{name, exact?, limit?}` args — added to `READ_ONLY_TOOLS`, returns a `symbols` kind result with the hits array, added to `autonomousCoding.summarizeStep` summarizer, system prompt tells the agent to prefer it over grep when looking for definitions. Three new tRPC procedures: `codeChat.findSymbols` (query), `symbolIndexStats` (query), `rebuildSymbolIndex` (mutation). Client-side `SymbolNavigatorPopover.tsx` is a VS-Code-style "Go to Symbol" modal bound to `Ctrl+T`/`Cmd+T` — opens with focus in the search input, fuzzy-searches via the tRPC query, renders results with kind icons + badges + file:line + one-line snippet, keyboard arrow navigation with Enter to select, clicking dispatches the existing `codechat-open-file` custom event so the FileBrowser loads the file at the right line. Added `Symbols` button to the config bar. Tool count badge flipped from `N/7` to `N/8` and `update_todos` + `find_symbol` both added to `DEFAULT_ENABLED_TOOLS`. `KeyboardShortcutsOverlay` documents the new Ctrl+T shortcut. New tests: 27 symbolIndex (extractSymbols per-kind coverage, line number capture, nested-scope skip, reserved-word filter, buildSymbolIndex aggregation + byName map, findSymbols ranking tiers, export boost, findExactName case-insensitive, stats) — pushing the codeChat surface to **580 passing across 25 files**. TS clean, 0 regressions, build 22.38s. **Passes 228-231 (Code Chat sharing + organization + context awareness)**: Pass 228 added session library JSON import with merge/replace modes — `importLibrary(existing, raw, mode)` pure function routes every entry through `parseLibrary()` so malformed sessions drop silently; merge mode skips sessions whose id already exists (last-write-loses from the user's perspective), replace mode discards the entire existing library. Symmetric with the existing Export library button. SessionsLibraryPopover grew a hidden `<input type="file">` + Import button that prompts "merge or replace" on each run. Pass 229 added session tags for organization — extended `SessionSnapshot` with an optional `tags?: string[]` field, `normalizeTag` pure function (trim/lowercase, strip leading #, reject whitespace/special chars except `_-/.`), `addTag`/`removeTag`/`allTags`/`filterByTags` helpers. `SessionsLibraryPopover` now renders a tag-filter chip row above the session list, per-session tag strips with inline tag input, and a Tag button in the row action bar. Filter chips AND-combine (session must match every selected tag). Pass 230 added a context window usage meter — `MODEL_CONTEXT_LIMITS` table covering all 8 routed models + a `DEFAULT_CONTEXT_LIMIT` (128K) fallback for unknown models; `evaluateContextWindow(totalTokens, model)` returns `{used, limit, pct, status, modelKnown}` with thresholds at 60% (warning) and 80% (critical). Config bar grew a `ctx NN%` pill that picks up the active model from `modelOverride` or the most recent assistant message, flipping to amber at 60% and red at 80% with a tooltip showing the exact used/limit bytes. Pass 231 added prompt template library JSON import/export — symmetric with the session import system. `exportTemplates` serializes user templates under a `{version, templates}` wrapper (built-ins are rehydrated from code), `parseTemplateExport` accepts either the wrapped or bare-array formats and strips any `builtin: true` overrides for safety. `importTemplates` in merge mode dedupes by `(name, body)` to prevent double-import, in replace mode keeps built-ins and swaps user templates. `PromptTemplatesPopover` footer grew Import/Export buttons; Export only renders when the user has custom templates. New tests: 6 importLibrary + 5 normalizeTag + 7 addTag/removeTag + 5 allTags/filterByTags + 9 context window + 10 template import/export — 42 additional tests pushing the codeChat surface to **368 passing across 17 files**. TS clean, 0 regressions. **Passes 224-227 (Code Chat workflow power features)**: Pass 224 added a pinned-files working set — `pinnedFiles.ts` pure-function store (parse/add/remove/toggle with dedup + 10-entry cap) + `applyPinnedToMessage` that prepends `@path` references to user messages before sending so the server-side `extractFileMentions` expander picks them up as auto-context. Pinned state persists to `localStorage['stewardly-codechat-pinned-files']`; a chips row above the chat input shows active pins with unpin buttons; a **Pin** button in the FileBrowser viewer dispatches `codechat-pin-file` custom events that the parent listens to (decouples FileBrowser from parent state). Pass 225 added grep result quick-jump — `grepMatches.ts` pure-function parser that extracts `{file, line, text}` entries from the SSE tool_result preview + `groupMatchesByFile` that sorts entries by file/line for rendering. `GrepResultView` renders clickable per-line rows under every `grep_search` trace step; clicking a hit dispatches `codechat-open-file` which hoists `activeTab` state in `CodeChatPage` to switch to the Files tab and stashes the pending-open target in localStorage for FileBrowser to pick up on mount. Pass 226 added an aggregate session stats dashboard — `aggregateSessions(library)` pure function counts total sessions/messages/tool-calls across the library, builds a tool-kind breakdown map, collects distinct models used, and tracks oldest/newest timestamps. `SessionsLibraryPopover` renders a compact stats strip above the save form with the totals + top-6 tool-kind chips. Pass 227 added Vim-style two-key chord shortcuts — `keyChords.ts` pure state machine (`stepChord` returns `{kind: pending|match|reset|ignore, next}`) implementing the g+{c,f,r,d,h,w,j} chord family for tab navigation with a 1.5s timeout. `CodeChatPage` now hoists `activeTab` into controlled state and attaches a window `keydown` listener that skips text fields and dispatches chord matches through `setActiveTab`. `KeyboardShortcutsOverlay` documents every chord. New tests: 21 pinnedFiles + 10 grepMatches + 3 aggregateSessions + 10 keyChords — 44 additional tests pushing the codeChat surface to **327 passing across 17 files**. TS clean, 0 regressions. **Passes 220-223 (Code Chat session power features)**: Pass 220 added conversation forking — `forkMessagesAt` pure-function helper (slices the message list inclusive of the target id, returns the original list unchanged when the id is missing) + a GitFork button in the hover action bar on every assistant message that calls a `handleForkAt` handler. Forking saves the truncated conversation as a new `SessionSnapshot` via `upsertSession`, loads it into the live hook via `loadMessages`, and sets `currentSessionId` to the new fork so future checkpoint saves attach to it. Pass 221 added cross-session full-text search — `searchSessions` pure-function matcher that scans every saved session, returns `SessionSearchHit[]` with `{sessionId, sessionName, messageIndex, messageRole, snippet, matchAt, matchLen}` ordered by session updatedAt newest-first, snippet is a ~120-char window around the match with `…` truncation markers; wired into `SessionsLibraryPopover` as a top-level search input that renders highlighted results above the session list with click-to-load. Pass 222 added a cost budget guardrail — `evaluateBudget` pure function returns `{status: ok|warning|blocked, pct, remainingUSD}` based on session total cost vs a per-user USD limit, warn threshold configurable (default 50%), block threshold at 100%; `BudgetState` persists to `localStorage['stewardly-codechat-budget']` across refreshes; config-bar token/cost pill flips to amber on warning and red on blocked with a tooltip showing "$X of $Y used · Z%", clicking the pill opens a prompt to set/clear the limit; `handleSend` short-circuits with a toast when `budgetEval.status === "blocked"` so users can't accidentally overspend. Pass 223 added silent auto-checkpoint — `shouldCheckpoint` pure function decides when to save (needs ≥1 assistant reply, delta ≥ everyN, everyN > 0); `useEffect` watches `messages` and calls `upsertSession` every 4 new messages with a stable auto-`${uuid}` session id so repeated saves update the same row instead of spamming the library. Reuses `currentSessionId` when set so manual saves and auto-checkpoints share the same snapshot. Resets state when `messages.length === 0` (after `/clear`). New tests: 12 forkMessagesAt + searchSessions + 6 shouldCheckpoint (18 sessionLibrary additions) + 8 evaluateBudget (tokenEstimator additions) — 26 additional tests pushing the codeChat surface to **283 passing across 14 files**. TS clean, 0 regressions. **Passes 216-219 (Code Chat terminal classics + power features)**: Pass 216 added a Ctrl+R reverse-i-search over the Code Chat command history — `commandHistorySearch.ts` pure-function matcher with exact substring ranking, subsequence fallback, early-position boost, and character-index tracking for UI highlighting; `CommandHistorySearchPopover.tsx` modal with search input, keyboard nav (Arrow up/down, Enter to insert, Esc to close), highlighted matches, and a footer showing `N / total` counts; Ctrl+R / Cmd+R hotkey wired globally (works from inside the textarea, no modifier conflict) and documented in `KeyboardShortcutsOverlay`. Pass 217 added a per-message tool-call summary receipt — `toolSummary.ts` aggregator (`summarizeToolEvents` + `summaryChips` + `summarySentence`) that reduces `msg.toolEvents` into compact chips (`read 4 · grep 2 · edit 1 · bash 1 · errors 1`) rendered under every assistant message with mutation/error color coding (`info` neutral, `warn` amber, `error` red) and a "N files" summary with a tooltip listing exact paths the agent touched. Pass 218 added `{{variable}}` placeholder substitution to the prompt template library — `extractTemplateVariables` pure function finds unique `{{name}}` placeholders in order (tolerates whitespace, rejects malformed), `applyTemplateVariables` substitutes known values and leaves unknowns as-is; `PromptTemplatesPopover.tsx` now detects variables on template pick and opens a form above the template list asking for values before insertion, with an "Insert raw" escape hatch that leaves placeholders intact. Template rows show a `N vars` badge when placeholders are present (hover tooltip lists variable names). Pass 219 added a "publish conversation to GitHub Gist" one-click flow — `createGist` helper in `githubClient.ts` wrapping `POST /gists` with the caller's PAT (secret by default so users can share via URL without accidental public broadcast); `codeChat.exportToGist` tRPC mutation validates the content payload (≤1MB), sanitizes the filename regex, logs `{userId, gistId, public}` via the standard logger; `Gist` button next to `Export` in the config bar publishes the current conversation, auto-derives the description from the first user message, copies the gist URL to clipboard on success, and surfaces a toast with an "Open" link. New tests: 13 commandHistorySearch (dedup, scoring, subsequence, highlight segments) + 12 toolSummary (kind counts, error detection, file collection, singular/plural sentence) + 9 promptTemplate variable tests + 2 createGist (POST body + 422 validation). Full codeChat surface: **257 passing across 14 files** (221 from pass 215 + 36 new). TS clean, 0 regressions. **Passes 212-215 (Code Chat session management + productivity + navigation)**: Pass 212 added a saved-sessions library (`client/src/components/codeChat/sessionLibrary.ts` pure-function store + `SessionsLibraryPopover.tsx` modal) with localStorage persistence — `upsertSession` dedupes by id + sorts newest-first, `deleteSession`/`renameSession` are pure, `autoName` derives a readable title from the first user prompt, MAX_SESSIONS=50 cap drops oldest on overflow, cross-tab refresh on popover open. Exposed `loadMessages()` on `useCodeChatStream` so loading a saved session swaps the active hook state in place. New Sessions button in the config bar + wired `/clear` to also reset `currentSessionId`. Pass 213 added fine-grained per-tool permission toggles — `ToolPermissionsPopover.tsx` + `DEFAULT_ENABLED_TOOLS`/`CODE_TOOL_SPECS` constants listing all 6 Code Chat tools with mutation flags, "Read-only preset" quick action; plumbed `enabledTools: string[]` through `useCodeChatStream` → SSE POST body → server/routes/codeChatStream.ts which intersects it with the role-based allowlist (non-admin can't enable write tools regardless of client payload). Shows `N/6` badge in the config bar. Pass 214 added a prompt template library (`promptTemplates.ts` pure functions + 5 built-in templates: review, refactor, explain, test, debug; `PromptTemplatesPopover.tsx` search/save-current/new/delete UI) — built-ins are read-only and cannot be overridden by localStorage, user templates persist via `saveTemplates`, `filterTemplates` is case-insensitive substring match on name+body, 100-template cap drops oldest user entries. Pass 215 added a hierarchical file tree + codebase stats in the Files tab — `fileTreeBuilder.ts` pure `buildFileTree` (sorts dirs before files at each level, dedupes duplicates) + `computeStats` (top languages by extension, top directories by file count, unique dir counter); `FileTreePanel.tsx` renders the tree with collapsible folders, stats strip (total files/dirs + top-5 language pills), click-to-open wired to the existing file viewer; expanded/list toggle in the Workspace card header; extended `codeChat.listWorkspaceFiles` with an `all: true` option that bypasses fuzzy filter and returns the full file list (cap raised from 50 to 5000). New tests: 21 sessionLibrary + 18 promptTemplates + 13 fileTreeBuilder — 52 additional tests pushing the codeChat surface to **221 passing across 12 files**. TS clean, 0 regressions. **Passes 207-211 (Code Chat polish — discoverability, telemetry, error recovery)**: Pass 207 added lazy-loaded Shiki syntax highlighting to `MarkdownMessage` code blocks (`client/src/components/codeChat/shikiHighlight.ts`) with bundled grammars for ts/tsx/js/json/bash/python/go/rust/yaml/etc, alias resolution (typescript→ts, py→python, yml→yaml), and a progressive-enhancement render: the plain `<pre>` shows immediately, then Shiki swaps in when the ~1MB WASM highlighter resolves. Pass 208 added an assistant-message action bar (Copy/Export/Regenerate) that appears on hover, built a `conversationExport.ts` pure-function markdown exporter (per-message + full-conversation formats with collapsible tool-trace `<details>`, metadata line, value truncation, `downloadTextFile` browser helper), added a `regenerateLast()` method to `useCodeChatStream` that trims back to the most-recent user turn and re-sends, and added an "Export" button to the config bar. Pass 209 added `KeyboardShortcutsOverlay` (`?`-triggered modal listing every shortcut + all 9 slash commands, ARIA dialog semantics, close on backdrop click), draft autosave to `localStorage['stewardly-codechat-draft']` (restores on mount, clears on empty), and a Keyboard icon button in the config bar. Pass 210 added client-side token/cost telemetry (`tokenEstimator.ts` with a chars/3.8 heuristic + `MODEL_PRICING` table for 8 known models) that shows per-message `Nkt ($0.000)` alongside iterations/duration and a running session total in the config bar. Pass 211 replaced the fire-and-forget error toast with a persistent `AlertTriangle` banner above the input that offers Retry (calls `regenerateLast`) and Dismiss buttons, auto-clears on new send. New deps: `shiki@3` promoted from transitive to direct. New tests: 5 shikiHighlight (`normalizeLanguage` alias mapping), 9 conversationExport (title, metadata, tool traces, truncation, single-message variants), 16 tokenEstimator (`estimateTokens` ratios, `sumUsage` with mixed priced/unpriced, formatters). Full codeChat surface: **169 passing across 9 files** (139 from pass 206 + 30 new). TS clean, 0 regressions. **Passes 202-206 (Code Chat terminal UI parity with Claude Code)**: five consecutive novel passes closing the remaining cloud-parity gaps on top of pass 201. Pass 202 built `shared/lineDiff.ts` — a pure-function O(N*M) LCS line differ with hunk grouping and `formatUnifiedDiff()`, plus `client/src/components/codeChat/DiffView.tsx` that renders a GitHub-style two-column gutter diff (green add / red delete / chart-3 hunk header) — wired into the Code Chat FileBrowser inline editor via a "Diff" toggle that flips between the Textarea view and a live preview of draft-vs-saved changes. Pass 203 added a slash-command registry + popover (`client/src/components/codeChat/slashCommands.ts` + `SlashCommandPopover.tsx`) with 9 built-in commands (`/clear`, `/cancel`, `/help`, `/write on|off`, `/iterations <n>`, `/model <id>`, `/diff <path>`, `/explain <path>`, `/find <pattern>`), keyboard nav (arrow up/down + Tab/Enter to select, Esc to close), rewrite-to-prompt support (diff/explain/find expand into a richer ReAct prompt), role gating (`/write` refuses non-admin), and Escape-to-abort during execution. Pass 204 added `MarkdownMessage.tsx` using `react-markdown` + `remark-gfm` to render assistant replies as rich markdown (headings, lists, GFM tables, fenced code with per-block Copy button and language label) — replacing the old monospace `<pre>` wrap. Pass 205 extended `WriteFileResult` and `EditFileResult` to carry 64KB `before`/`after` snapshots (capped via `diffTruncated` flag) so the SSE tool-result events can stream them to the client; `TraceView` now auto-detects edit_file/write_file results, parses the JSON preview, and renders an inline `DiffView` instead of the raw JSON dump. Pass 206 built the `@file` mention system: `server/services/codeChat/fileIndex.ts` walks the workspace with a 60s cached file list (skips `node_modules`/`dist`/`.git`/binaries/lockfiles, returns POSIX paths) + `fuzzyFilterFiles` basename-first ranking + `extractFileMentions` that pulls `@path` and `@{bracketed path}` refs from a message (skips bare `@usernames`, caps at 5 refs); exposed via `codeChat.listWorkspaceFiles` tRPC query; client-side `FileMentionPopover.tsx` renders on `@query` typed in the input with Tab/Enter select, and the SSE endpoint automatically reads the mentioned files and inlines their contents at the bottom of the user message so the LLM gets them without needing a separate `code_read_file` round-trip (emits a `mentions_resolved` SSE event for the UI). New tests: 15 `lineDiff`, 28 `slashCommands`, 17 `fileIndex`, 3 new fileTools before/after snapshot tests — 63 additional tests pushing the codeChat surface to 139 passing across 6 files. Full suite total: 3,191 passing across 112 files. TS clean, 0 regressions. **Pass 201 (Claude Code cloud parity + multi-repo GitHub write access + background jobs)**: expanded `server/services/codeChat/githubClient.ts` with the write-side of the GitHub REST API — `listUserRepositories`, `listBranches`, `commitMultipleFiles` (full 5-step blob→tree→commit→ref Git Data API flow for atomic multi-file pushes), `mergePullRequest` (merge/squash/rebase), `updatePullRequest`, `getPullRequest` (mergeable state detail), `deleteBranch`, `getAuthenticatedUser`, and `verifyPushAccess` as a pre-flight permission gate. Expanded `server/routers/codeChat.ts` with 11 new user-scoped tRPC procedures (`githubMe`, `githubListMyRepos`, `githubListBranches`, `githubListPullRequests`, `githubGetPullRequest`, `githubGetFile`, `githubCreateBranch`, `githubCommitFiles`, `githubCreatePullRequest`, `githubUpdatePullRequest`, `githubMergePullRequest`, `githubDeleteBranch`) — every one gated through `loadGitHubCredentialsForUser(ctx.user.id)` so users act as themselves, not a shared deployment token, and so their GitHub token's own scope is the only access boundary. Added `server/services/codeChat/backgroundJobs.ts` — in-memory background job store with cooperative-cancel semantics, per-user concurrency cap (2 running), history trimming (50 terminal), event-log ring buffer, and the `enqueueJob`/`listJobs`/`getJob`/`cancelJob`/`jobStats` surface. Exposed `listBackgroundJobs`/`getBackgroundJob`/`cancelBackgroundJob`/`startAutonomousJob` (admin)/`startGitHubPushJob` (user) tRPC procedures. Built two new client components — `client/src/components/codeChat/GitHubWritePanel.tsx` (multi-repo picker + branches card + commit-&-push card with inline file loading + open-PR list with merge/squash buttons + create-PR card) and `client/src/components/codeChat/BackgroundJobsPanel.tsx` (active jobs with live polling, history, per-job event log, cancel button, admin-only start-autonomous form). Added two new tabs (`Git Write`, `Jobs`) to `client/src/pages/CodeChat.tsx` and wired inline editing into the FileBrowser viewer (admin Edit/Save/Cancel with Textarea replacing the `<pre>` view and direct write_file dispatch). New tests: `server/services/codeChat/githubClient.test.ts` (17 tests covering every new primitive against a stubbed fetch), `server/services/codeChat/backgroundJobs.test.ts` (8 tests covering enqueue/run/cancel/concurrency/isolation). Full suite: **3,128 passing across 111 files** (baseline 3,103 + 25 new tests, same 14 env-dependent failing files — 0 regressions). TS clean, build 20.55s. **Recursive optimization declared complete.**

## Wealth Engine (Phases 1-7 + Rounds A/B/C/D/E, see docs/WEALTH_ENGINE.md + docs/CONSENSUS.md + docs/ENGINES_MIGRATION.md)
Phase 1-7 ported the WealthBridge v7 HTML calculator engines into TypeScript and wired them into the full Stewardly stack. Round A added natural-language chat dispatch, PDF download buttons, per-message chat actions, and 3 synthesizer pulls. Round B added a Claude-Code-style code chat foundation. Round C added multi-model consensus stream + weight presets + UI trio. Round D wired the live SSE endpoint at /api/consensus/stream, added a pre-flight cost+latency badge, and a Chat→Consensus deep link. Round E lifted the Round C trio inline into Chat.tsx consensus mode, added LLM-as-judge semantic agreement scoring, shipped the weight_presets SQL migration (0009_weight_presets.sql), and documented the two-stack engine dedup path (docs/ENGINES_MIGRATION.md). 656 tests across 12 files (583 wealth-engine + 54 parallel main-branch engines + 19 semantic agreement). 41 recursive optimization passes converged at 9.7-9.8/10.
- `server/shared/calculators/` — UWE/BIE/HE/Monte Carlo/benchmarks (Phase 1)
- `server/routers/wealthEngine.ts` — 30+ tRPC procedures (Phase 2A + Round A6 + Round C2/C4)
- `server/services/agent/calculatorOrchestrator.ts` — agent workflow chains (Phase 2B)
- `server/services/ghl/` — GoHighLevel CRM integration (Phase 3)
- `client/src/pages/wealth-engine/` — React UI (Retirement, StrategyComparison, PracticeToWealth, QuickQuote) (Phase 4)
- `server/services/wealthEngineReports/` — 4 PDF templates + Edge TTS audio narration + shareable links (Phase 5). Consumed by `client/src/components/wealth-engine/DownloadReportButton.tsx` on StrategyComparison, Retirement, and `client/src/pages/EngineDashboard.tsx` (pass 49) via the `wealthEngine.generateReport` tRPC mutation (complete_plan template).
- `server/services/wealthChat/` — 5 chat tools + safety wrappers + 5 proactive triggers + chat-engine dispatcher (Phase 6 + Round A3)
- `server/services/improvement/` — Plaid perception + 6 improvement loops (Phase 7)
- `server/services/synthesizer/` — wordDiff (LCS) + timeBudgetSelector + costEstimator (Round A7)
- `server/services/codeChat/` — fileTools + executor + autonomousCoding + roadmapPlanner + githubClient (Round B). githubClient uses `GITHUB_TOKEN` env PAT fallback (pass 54) via `loadGitHubCredentialsFromEnv()` + `getDefaultRepo()`.
- `server/routers/codeChat.ts` + `client/src/pages/CodeChat.tsx` — admin file browser + roadmap CRUD + word-diff visualizer (Round B5) + GitHub integration status/PR tab (pass 54). Routed in ADMIN_NAV (`/code-chat`, admin only). tRPC procs `codeChat.githubStatus` + `codeChat.githubListOpenPRs` gate on `adminProcedure`. Pass 58: roadmap now persists to `.stewardly/roadmap.json` via `loadRoadmap()` + `setRoadmap()` so admin edits survive server restarts (path overridable via `CODE_CHAT_ROADMAP_PATH`). `.stewardly/` is gitignored.
- `server/services/consensusStream.ts` — runConsensus + streamConsensus + buildSynthesisPrompt + parseSynthesisOutput (Round C2) + combined semantic/Jaccard agreement (Round E2)
- `server/services/weightPresets.ts` — per-model weight CRUD with 5 built-in presets (Round C4)
- `server/services/semanticAgreement.ts` — LLM-as-judge semantic agreement scorer, falls back to Jaccard on failure (Round E2)
- `client/src/components/consensus/` — StreamingResults + TimingBreakdown + ComparisonView (Round C3)
- `client/src/pages/Consensus.tsx` — multi-model consensus demo page (Round C3)
- `client/src/pages/Chat.tsx` — consensus mode inlines the Round C3 trio via wealthEngine.consensusStream (Round E1), with graceful fallback to legacy advancedIntelligence.consensusQuery
- `drizzle/0009_weight_presets.sql` — SQL migration for the weight_presets table (Round E3), **applied** to production database
- `server/shared/calculators/scui.ts` — SCUI module ported from engines/ to canonical shared/calculators (engine dedup pass 53)
- `docs/ENV_SETUP.md` — Setup guide for optional environment variables (FRED_API_KEY, CENSUS_API_KEY)

## EMBA Learning Integration (April 2026, see docs/EMBA_INTEGRATION.md)
Merged the EMBA Knowledge Explorer into Stewardly under the `learning.*` namespace. 30 new `learning_*` tables, one tRPC router with 6 subrouters (mastery / licenses / content / freshness / recommendations / seed + importFromGitHub), 11 new ReAct agent tools, 3 new React pages (Learning Home, License Tracker, Content Studio), and a full dynamic content CRUD system with versioning + audit trail.

**Content state:** The initial `seed.ts` module populates the structural skeleton only — 8 core disciplines + 12 canonical exam tracks. The full content payload (366+ definitions, chapters, subsections, practice questions, flashcards) lives in the public [`mwpenn94/emba_modules`](https://github.com/mwpenn94/emba_modules) repo and is pulled on demand via `server/services/learning/embaImport.ts` (pass 54) — either at server boot (set `EMBA_IMPORT_ON_BOOT=true`) or manually via the **Import from GitHub** button in `/learning/studio` (admin only). Every insert is dedup-gated so re-runs are safe. Unit-tested via `embaImport.test.ts` (5 tests, all passing).

49 new unit tests across 7 files (14 permissions + 6 mastery + 8 licenses + 8 freshness + 8 recommendations + 5 embaImport + wiring smoke). Task coverage: 1 (schema+auth), 2 (router+agent), 3 (regulatory pipeline), 4 (UI consolidation+nav), 5 (agentic licensure), 6 (page migration skeleton), 7 (dynamic content CRUD+permissions+seed), 8 (GitHub content import — pass 54).
- `drizzle/schema.ts` — 30 new learning_* tables (mastery, sessions, licenses, CE credits, regulatory, tracks, definitions, formulas, cases, fs_applications, connections, chapters, subsections, questions, flashcards, content_history, ai_quiz_questions, playlists, groups, bookmarks, discovery, ...)
- `drizzle/0010_emba_learning.sql` — idempotent SQL migration (CREATE TABLE IF NOT EXISTS)
- `server/services/learning/permissions.ts` — RBAC model (canEditContent/canPublish/canSeedContent/canSeeContent) + 14 tests
- `server/services/learning/mastery.ts` — SRS with pure scheduleNextReview (0-5 confidence ladder) + 6 tests
- `server/services/learning/licenses.ts` — licensure CRUD + CE credits + deriveLicenseAlerts (expiration_warning / ce_credits_needed / expired) + 8 tests
- `server/services/learning/content.ts` — full CRUD on definitions/tracks/chapters/subsections/questions/flashcards + audit trail + unified search + explainConcept
- `server/services/learning/seed.ts` — idempotent skeleton seed for 8 core disciplines + 12 canonical exam tracks (SIE/Series7/Series66/CFP/Financial Planning/Investment Advisory/Estate Planning/Premium Financing/Life&Health/General Insurance/P&C/Surplus Lines). Does NOT insert definitions/chapters/questions — see `embaImport.ts` for the full payload.
- `server/services/learning/embaImport.ts` — pure-fetch importer that pulls `emba_data.json` + `tracks_data.json` from `mwpenn94/emba_modules` and hydrates definitions, disciplines, tracks, chapters, subsections, practice questions, and flashcards (pass 54). No auth required (source repo is public). Every insert dedup-gates by slug/term/title. Wired to `learning.importFromGitHub` tRPC mutation + Content Studio "Import from GitHub" button.
- `server/services/learning/freshness.ts` — REGULATORY_SOURCES catalog (FINRA/NASAA/CFP Board/IRS/NAIC/State DOI) + checksum-based onContentSourceUpdated + recordRegulatoryUpdate + admin review workflow + 8 tests
- `server/services/learning/recommendations.ts` — pure fuseRecommendations algorithm (SRS due > CE > expiration > calculator-informed > broadening) + CALCULATOR_TRACK_MAP + 8 tests
- `server/services/learning/agentTools.ts` — registers 11 ReAct tools: check_license_status, recommend_study_content, assess_readiness, generate_study_plan, explain_concept, quiz_me, check_exam_readiness, generate_flashcards, generate_practice_questions, suggest_content_improvements, draft_definition
- `server/services/learning/bootstrap.ts` — startup hook wiring (seed + tool registration, idempotent)
- `server/routers/learning.ts` — 47+ tRPC procedures organized as learning.mastery.* / licenses.* / content.* / freshness.* / recommendations.* / seed (pass 58 added `content.listSubsections` + `content.importFromGitHub`)
- `client/src/pages/learning/LearningHome.tsx` — role-aware dashboard: mastery snapshot, license tracker summary, agent recommendations, exam tracks grid (track cards link to LearningTrackDetail)
- `client/src/pages/learning/LearningTrackDetail.tsx` (pass 58) — track reader with lazy-loaded chapter → subsection content + counts for flashcards + practice questions + jump buttons into the two study flows
- `client/src/pages/learning/LearningFlashcardStudy.tsx` (pass 58) — flip-card study UI that wires correct/incorrect answers into `learning.mastery.recordReview` so the SRS scheduler actually moves confidence on the 0-5 ladder
- `client/src/pages/learning/LearningQuizRunner.tsx` (pass 58) — multiple-choice quiz runner with correct-answer highlight, explanation panel, and the same SRS wiring for practice questions
- `client/src/pages/learning/LicenseTracker.tsx` — license grid + add form + CE progress bars + alerts panel
- `client/src/pages/learning/ContentStudio.tsx` — advisor+ authoring hub: draft definition form, recent drafts list, admin-only regulatory review queue + seed button
- `client/src/lib/navigation.ts` — added Learning / Licenses (user) and Content Studio (advisor) nav entries
- `client/src/App.tsx` — 7 new routes under `/learning/*`
- `server/_core/index.ts` — server startup calls `bootstrapLearning()` after `initScheduler()`

## Commands
`node toolkit.js init stewardly --safety` — Initialize (run once)
`node toolkit.js verify` — Pre-pass ledger check
`node toolkit.js snapshot <N>` — Save pre-pass state
`node toolkit.js score <N> "<6 scores>" <type> <novelty>` — Record scores
`node toolkit.js suggest` — Get AI-recommended next pass type
`node toolkit.js fail "<desc>"` — Log failed approach (prevents re-attempt)
`node toolkit.js check-gaming` — Detect evaluation gaming (every 3rd pass)
`node toolkit.js status` — Full dashboard
`node toolkit.js diverge <name>` — Create branch
`node toolkit.js converge <name>` — Merge winning branch
`node toolkit.js prune <name> "<reason>"` — Reject branch with decision record

## Workflow (every pass)
`node toolkit.js verify && node toolkit.js snapshot <N>`
Execute the pass (see optimization prompt below)
`node toolkit.js score <N> "<C,A,D,N,Ac,R>" <type> <novelty>`
If approach failed: `node toolkit.js fail "<what failed and why>"`
If branch work: `node toolkit.js diverge|converge|prune` as needed
Every 3rd pass: `node toolkit.js check-gaming`

## Resolved (formerly Known Gaps)
- ~~chat.send bypasses contextualLLM~~ → RESOLVED: chat.send uses contextualLLM via ReAct loop
- ~~graduatedAutonomy in-memory Map~~ → RESOLVED: DB-backed via agent_autonomy_levels + write-through cache
- ~~Single-turn tool calling~~ → RESOLVED: ReAct multi-turn loop with 5 max iterations + trace logging
- ~~Chrome extension: spec only~~ → RESOLVED: Full extension with LinkedIn capture, Gmail compliance, side panel
- ~~Conversation branching: not implemented~~ → RESOLVED: Fork button + BranchComparison.tsx
- ~~Chat Loop/Consensus not wired~~ → RESOLVED: Loop wired to autonomousProcessing.start, Consensus to advancedIntelligence.consensusQuery
- ~~Rich media/ads/video not in UI~~ → RESOLVED: RichMediaEmbed.tsx, ContextualAd.tsx, video streaming layout
- ~~RichMediaEmbed component existed but was never rendered in chat~~ → RESOLVED: wired into Chat.tsx message rendering from `msg.metadata.mediaEmbeds` with client-side text fallback
- ~~extractMediaFromResponse/storeMediaEmbeds defined but never called~~ → RESOLVED: invoked from sseStreamHandler `done` events, persistStreamed, chat.send, and rehydrated via conversations.messages on load
- ~~Loop mode only used the first selected focus~~ → RESOLVED: autonomousProcessing accepts `foci[]` and cycles round-robin across selected foci per iteration
- ~~No "loop previous prompt" / loop-by-type~~ → RESOLVED: "↻ Loop previous" button replays last user message through loop config; free-text "Prompt type" field tags the run and is passed into the model prompt as context
- ~~Workflow UI missing~~ → RESOLVED: /workflows page with 5 templates and run/progress tracking
- ~~No URL hallucination guardrail~~ → RESOLVED: server/shared/guardrails/urlHallucination.ts detects/strips fabricated URLs; trusted domain allowlist; 19 tests
- ~~ragTrainer.ts TODO for episodic aggregation~~ → RESOLVED: implemented via contextualLLM (summarizes 20+ episodic memories, keeps 5 most recent, inserts aggregated summary)
- ~~CSP nonce tests failing~~ → RESOLVED: comment reword in server/_core/index.ts
- ~~invokeLLM bypass in ragTrainer~~ → RESOLVED: refactored to use contextualLLM (complies with wiring verification tests)
- ~~invokeLLM bypass in pdfReportGenerator (Phase 7 regression)~~ → RESOLVED (pass 45): refactored to use contextualLLM via shared/stewardlyWiring; wiring-verification test passes
- ~~Messages table missing parentMessageId column~~ → RESOLVED: ALTER TABLE migration applied
- ~~Loop mode required foci selection~~ → RESOLVED: added 'general' focus for plain iteration without cycling
- ~~Flaky consolidatedPhase3 timeout~~ → RESOLVED: increased describe timeout to 30s for resource contention

## Known Gaps (current — human action required)
- CRM credentials not configured (GHL, Wealthbox, Redtail — services ready)
- Optional env vars (FRED_API_KEY, CENSUS_API_KEY) not yet set
- INTEGRATION_ENCRYPTION_KEY needs generation (openssl rand -hex 32)
- Compliance review not yet performed (FINRA 2210, CAN-SPAM, TCPA, CCPA, Reg BI, Fair Lending)
- ~~2 pre-existing CSP nonce infrastructure tests fail~~ → RESOLVED: comment reword removed literal 'unsafe-inline' string

## Intelligence Layer (wired and functional)
- contextualLLM: RAG-enabled with guardrails (PII + injection screening on all I/O)
- 5-layer config: platform → organization → manager → professional → user
- Multi-model: 23 models via model registry (Gemini, GPT, Claude, Reasoning, Llama, Mistral, Mixtral, Qwen), task routing, MODEL SELECTOR in Chat UI with multi-select consensus mode
- Chat modes: Single (normal) / Loop (autonomous diverge/converge with 5 foci) / Consensus (multi-model)
- Autonomous processing: user-driven diverge/converge loops with 5 foci (discovery/apply/connect/critique/general), budget-capped, integrated into Chat UI
- Usage tracking: every LLM call logged with tokens + cost estimation
- Graduated autonomy: DB-backed via agent_autonomy_levels + write-through cache
- ReAct loop: multi-turn tool calling with trace logging (5 max iterations)
- Web search: google_search (Forge native) + web_search (Tavily/Brave fallback)
- Improvement engine: signal detection on 6h schedule
- SSE streaming: POST /api/chat/stream with real token streaming
- Voice/TTS: Edge TTS via msedge-tts, 25+ neural voices, voice router in tRPC
- Memory engine: 6 categories, episodic summaries, context injection
- Event bus: prompt.scored, compliance.flagged, goal.completed
- OpenTelemetry: GenAI spans on every contextualLLM call
- MCP server: 6 financial tools at /mcp/sse + /mcp/call
- Multi-tenant: tenantId in tRPC context + AsyncLocalStorage middleware
- Agents: 8-part agentic execution (gate review, quotes, applications, advisory, estate, PF, carrier, compliance)
- Workflows: onboarding checklist system (5 procedures) + workflow automation engine (5 predefined multi-step workflows: onboarding, annual review, compliance, lead nurture, reports)
- Consensus LLM: multi-model query for high-stakes recommendations (via advancedIntelligence router)
- Financial planning agent: 5-step orchestration with $2 budget cap (via advancedIntelligence router)
- Batch pipeline: bulk enrich/score for lead pipeline (via advancedIntelligence router)
- Report export: PDF/markdown with FINRA 17a-4 archiving (via advancedIntelligence router)
- Feedback: thumbs up/down ratings wired to advancedIntelligence.rateResponse
- Template optimizer: monthly model-per-domain testing (scheduled)
- Autonomous analysis: nightly client gap analysis with $0.50/client budget (scheduled)
- Autonomous training: uses excess free capacity every 4h to run RAG training, template optimization, bias checks
- RAG trainer: learns from every LLM response (fact extraction → user_memories, tool patterns → episodic)
- URL hallucination guardrail: detects/strips fabricated URLs in AI responses; allows trusted domains (IRS, SEC, FINRA, YouTube, etc.)
- OpenClaw agents: CRUD agent instances, compliance-aware execution, reads/stores/trains on compliance data. Pass 58: every run now persists a row in `agent_actions` (actionType, dataAccessedSummary, dataModifiedSummary, durationMs, errorMessage) and increments `agent_instances.totalActions` + `totalCostUsd` so the AgentManager UI "0 runs, $0.00" counter reflects real activity. `<AgentRecentRuns />` expansion panel in `client/src/pages/AgentManager.tsx` calls `openClaw.listActions` (owner-gated) and auto-refreshes every 5s.
- Multi-model consensus: queries genuinely different models (Claude + GPT + Gemini) through Forge API
- Consensus UI: expandable panel showing agreement %, individual model responses, unique details per model

## UI Components (completed in final session)
- RichMediaEmbed.tsx: video (YouTube w/ timestamp), audio, images, documents, shopping, charts
- ContextualAd.tsx: contextual banners with Sponsored label, dismiss, respects adPolicy config (max 1/5 messages, 3/session cap)
- Video streaming layout: 70% video + chat overlay, auto-adapts for screen share/camera/co-browse
- BranchComparison.tsx: conversation branching with fork button on assistant messages
- LeadCaptureGate: wrapping calculator results on EstatePlanning, TaxPlanning, RiskAssessment
- Workflows page: /workflows with 5 predefined templates, run/progress tracking, step visualization

## Business Domains
- Lead pipeline: capture, enrichment, propensity scoring, qualification, distribution
- Propensity: 3-phase scoring (expert → logistic → gradient boosting), 14 segment models, bias auditing
- Import engine: CSV/Dripify/Sales Nav parsers, field mapping, PII encryption, dedup
- CRM: GoHighLevel V2 + abstract adapter + Wealthbox + Redtail
- Verification: 7 providers (SEC, FINRA, CFP, NASBA, NMLS, state bar, NIPR)
- Reporting: pipeline health, performance, campaign ROI with snapshot persistence
- Planning: business plans, production actuals, weekly AI variance analysis
- Community: professional forum with posts and replies
- Premium finance: SOFR rates via FRED API
- Financial protection score: 12-dimension consumer suitability
- Compliance: FINRA 17a-4 communication archive, CAN-SPAM consent, TCPA opt-out

## Safety
This is a SAFETY-SENSITIVE project (financial advisory). Max 3 consecutive passes without human verification. All changes to recommendation logic require audit trail.

## Custom Domain Passes
When working on client-facing features, also run these:

**Compliance pass** — Check: SEC/FINRA requirements for automated advice, suitability standards, disclosure requirements, record-keeping (all recommendations logged and auditable?), data privacy (PII, SOC 2, encryption at rest), AML (transaction monitoring, KYC). Output: compliance gap register with regulatory citation.

**Fiduciary pass** — Check: Does system recommend lowest-cost when equivalent? Does graduated autonomy prevent actions against client interest? Are conflicts visible and managed? Can client challenge a recommendation? Output: fiduciary alignment score per capability.

**Cost pass** — Check: Token consumption per operation, semantic cache hit rates, cost per interaction, cost scaling at 10x/100x, provider cost comparison for routing. Output: cost model with projections + optimization recommendations.

## Recursive Optimization Prompt
Apply the Universal Holistic Optimization Prompt v4 to this project.
Starting temperature: 0.5 (Mid-stage maturity + Technical type)
See @docs/Universal_Holistic_Optimization_Prompt_v4.md for the complete prompt
When temperature drops and no alternatives explored yet, trigger Exploration pass
Use Sequential Halving for branch resolution (drop weakest first)
Score DIVERSITY separately from QUALITY when comparing branches
Log failed approaches with `node toolkit.js fail`
SCORING BIAS: Self-scores inflate by 0.5-0.7 points. Err conservative.
