Pass 104 Diagnosis Notes
========================
Issue: Nested <li> inside <li> causing React hydration errors on multiple pages.
This is a DOM nesting violation in the sidebar navigation (DashboardLayout or PersonaSidebar5).

Affected pages: Settings, Study Buddy, Documents, Admin Audit Trail
Root cause: A navigation component is rendering <li> elements nested inside other <li> elements.
This is likely in the sidebar where nav items with sub-items create nested lists.

Also: Study Buddy has a 404 resource load failure (likely a missing API endpoint or asset).

Need to check:
1. PersonaSidebar5.tsx - likely has nested <li> in nav items
2. DashboardLayout.tsx - sidebar rendering
3. StudyBuddy.tsx - 404 resource

Now need to test each Wealth Engine panel individually since the automated test couldn't click them.
